<h1>Listing questions</h1>

<div class="field">
	<%= render @questions %>
	<%= will_paginate @questions %>

	<div class"addlink">
	<%= link_to 'Back', project_path(params[:project_id]) %>
	
	</div>
</div>
